@model IEnumerable<VentasSistemasUni.Models.TipoProducto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreTipoProducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreTipoProducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoProductoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoProductoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoProductoID })
        </td>
    </tr>
}

</table>
